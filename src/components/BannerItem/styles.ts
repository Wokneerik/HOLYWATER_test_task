import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  bannerContainer: {
    width: 350,
    marginHorizontal: 10,
    borderRadius: 10,
    overflow: 'hidden',
  },
  bannerImg: {
    width: '100%',
    height: 200,
    borderRadius: 10,
    resizeMode: 'cover',
  },
});

export default styles;
