import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    paddingLeft: 16,
  },
  backgroundImage: {
    width: '100%',
    height: '100%',
  },
});

export default styles;
